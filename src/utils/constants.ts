/**
 * The default language option that is used in i18n initialization
 */
export const DEFAULT_LANGUAGE = 'en';

export const APP_LANGUAGE_ASYNC_STORAGE = 'appLanguage'; // key for async storage

/**
 * Constant that includes the language options for the language
 * select picker in Login screen {@see Login} {@see LanguagePicker}
 */
export const LANGUAGES = {
  EN: 'en',
  EL: 'el',
};

export const NATIVE_APP = 'native';

/**
 * Array to map the months in MMM format
 */
export const MONTHS = [
  'Jan',
  'Feb',
  'Mar',
  'Apr',
  'May',
  'Jun',
  'Jul',
  'Aug',
  'Sep',
  'Oct',
  'Nov',
  'Dec',
];

/**
 * Object that maps the currency to the appropriate sign
 */
export const CURRENCY_SIGN_RESOLVER = {
  euro: '€',
  dollar: '$',
};
