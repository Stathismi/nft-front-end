import styled from 'styled-components/native';
import assets from 'src/assets/images/logos';

export const LogoWithTitle = styled.Image`
  height: ${assets.logoWithTitle.height}px;
  width: ${assets.logoWithTitle.width}px;
  align-self: center;
  margin-bottom: 5px;
  margin-top: 40px;
`;
